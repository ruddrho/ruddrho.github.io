import { motion } from 'framer-motion'
import { FiCpu, FiCode, FiActivity, FiBox, FiEye } from 'react-icons/fi'
import { SectionTitle } from '../components/SectionTitle'
import { skillGroups } from '../data/portfolio'
const icons = [FiCpu, FiActivity, FiCode, FiBox, FiEye]
export function Skills() { return <section id="skills" className="section-wrap"><SectionTitle eyebrow="02 // Technical Stack" title="Systems thinking across the robotics pipeline." text="Core areas are organized around the full sense–plan–control–act loop."/><div className="grid gap-4 md:grid-cols-2 lg:grid-cols-3">{skillGroups.map((g,i) => { const Icon=icons[i]; return <motion.div key={g.title} initial={{opacity:0,y:25}} whileInView={{opacity:1,y:0}} viewport={{once:true}} transition={{delay:i*.06}} whileHover={{y:-5}} className="group glass-card relative overflow-hidden p-6"><div className="absolute -right-10 -top-10 h-28 w-28 rounded-full bg-cyan-300/5 blur-2xl transition group-hover:bg-purple-400/10"/><div className="flex items-center justify-between"><div className="grid h-11 w-11 place-items-center rounded-xl border border-cyan-300/20 bg-cyan-300/[.05] text-xl text-cyan-300"><Icon/></div><span className="font-mono text-[10px] tracking-[.2em] text-slate-600">{g.code}</span></div><h3 className="mt-5 text-xl font-medium text-white">{g.title}</h3><div className="mt-5 flex flex-wrap gap-2">{g.skills.map(s=><span key={s} className="tech-chip">{s}</span>)}</div></motion.div>})}</div></section> }
